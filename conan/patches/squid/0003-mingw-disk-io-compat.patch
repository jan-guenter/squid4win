diff --git a/pristine/squid-7.5/src/DiskIO/AIO/AIODiskFile.cc b/patched/squid-7.5/src/DiskIO/AIO/AIODiskFile.cc
index affd439..315f8be 100644
--- a/pristine/squid-7.5/src/DiskIO/AIO/AIODiskFile.cc
+++ b/patched/squid-7.5/src/DiskIO/AIO/AIODiskFile.cc
@@ -55,7 +55,7 @@ void
 AIODiskFile::open(int flags, mode_t, RefCount<IORequestor> callback)
 {
     /* Simulate async calls */
-#if _SQUID_WINDOWS_
+#if _SQUID_WINDOWS_ || _SQUID_MINGW_ /* squid4win-mingw-aio-disk-file */
     fd = aio_open(path.termedBuf(), flags);
 #else
     fd = file_open(path.termedBuf(), flags);
@@ -203,7 +203,7 @@ void
 AIODiskFile::close ()
 {
     assert (!closed);
-#if _SQUID_WINDOWS_
+#if _SQUID_WINDOWS_ || _SQUID_MINGW_ /* squid4win-mingw-aio-disk-file */
     aio_close(fd);
 #else
     file_close(fd);
diff --git a/pristine/squid-7.5/src/DiskIO/AIO/aio_win32.cc b/patched/squid-7.5/src/DiskIO/AIO/aio_win32.cc
index 88c5bf2..954e3b5 100644
--- a/pristine/squid-7.5/src/DiskIO/AIO/aio_win32.cc
+++ b/patched/squid-7.5/src/DiskIO/AIO/aio_win32.cc
@@ -16,8 +16,9 @@
 #include "win32.h"
 
 #include <cerrno>
+#include <cstdint> /* squid4win-mingw-aio-intptr */
 
-#if _SQUID_WINDOWS_
+#if _SQUID_WINDOWS_ || _SQUID_MINGW_ /* squid4win-mingw-aio-win32 */
 VOID CALLBACK IoCompletionRoutine(DWORD dwErrorCode,
                                   DWORD dwNumberOfBytesTransfered, LPOVERLAPPED lpOverlapped)
 {
@@ -293,7 +294,7 @@ int aio_open(const char *path, int mode)
                            NULL                     /* handle to template file */
                           )) != INVALID_HANDLE_VALUE) {
         ++ statCounter.syscalls.disk.opens;
-        fd = _open_osfhandle((long) hndl, 0);
+        fd = _open_osfhandle(reinterpret_cast<intptr_t>(hndl), 0);
         commSetCloseOnExec(fd);
         fd_open(fd, FD_FILE, path);
     } else {
diff --git a/pristine/squid-7.5/src/DiskIO/DiskThreads/aiops_win32.cc b/patched/squid-7.5/src/DiskIO/DiskThreads/aiops_win32.cc
index 2ee8f73..3a2fa52 100644
--- a/pristine/squid-7.5/src/DiskIO/DiskThreads/aiops_win32.cc
+++ b/patched/squid-7.5/src/DiskIO/DiskThreads/aiops_win32.cc
@@ -12,7 +12,9 @@
 #include "DiskIO/DiskThreads/CommIO.h"
 #include "DiskThreads.h"
 #include "fd.h"
+#include "mem/Allocator.h" /* squid4win-mingw-mem-allocator */
 #include "mem/Pool.h"
+#include "win32.h"
 #include "SquidConfig.h"
 #include "Store.h"
 
@@ -134,6 +136,74 @@ done_requests = {
 
 static HANDLE main_thread;
 
+static void
+squidaio_maperror(unsigned long win32Error) /* squid4win-mingw-maperror */
+{
+    static const struct {
+        unsigned long win32Code;
+        int posixErrno;
+    } errorTable[] = {
+        {ERROR_INVALID_FUNCTION, EINVAL},
+        {ERROR_FILE_NOT_FOUND, ENOENT},
+        {ERROR_PATH_NOT_FOUND, ENOENT},
+        {ERROR_TOO_MANY_OPEN_FILES, EMFILE},
+        {ERROR_ACCESS_DENIED, EACCES},
+        {ERROR_INVALID_HANDLE, EBADF},
+        {ERROR_ARENA_TRASHED, ENOMEM},
+        {ERROR_NOT_ENOUGH_MEMORY, ENOMEM},
+        {ERROR_INVALID_BLOCK, ENOMEM},
+        {ERROR_BAD_ENVIRONMENT, E2BIG},
+        {ERROR_BAD_FORMAT, ENOEXEC},
+        {ERROR_INVALID_ACCESS, EINVAL},
+        {ERROR_INVALID_DATA, EINVAL},
+        {ERROR_INVALID_DRIVE, ENOENT},
+        {ERROR_CURRENT_DIRECTORY, EACCES},
+        {ERROR_NOT_SAME_DEVICE, EXDEV},
+        {ERROR_NO_MORE_FILES, ENOENT},
+        {ERROR_LOCK_VIOLATION, EACCES},
+        {ERROR_BAD_NETPATH, ENOENT},
+        {ERROR_NETWORK_ACCESS_DENIED, EACCES},
+        {ERROR_BAD_NET_NAME, ENOENT},
+        {ERROR_FILE_EXISTS, EEXIST},
+        {ERROR_CANNOT_MAKE, EACCES},
+        {ERROR_FAIL_I24, EACCES},
+        {ERROR_INVALID_PARAMETER, EINVAL},
+        {ERROR_NO_PROC_SLOTS, EAGAIN},
+        {ERROR_DRIVE_LOCKED, EACCES},
+        {ERROR_BROKEN_PIPE, EPIPE},
+        {ERROR_DISK_FULL, ENOSPC},
+        {ERROR_INVALID_TARGET_HANDLE, EBADF},
+        {ERROR_WAIT_NO_CHILDREN, ECHILD},
+        {ERROR_CHILD_NOT_COMPLETE, ECHILD},
+        {ERROR_DIRECT_ACCESS_HANDLE, EBADF},
+        {ERROR_NEGATIVE_SEEK, EINVAL},
+        {ERROR_SEEK_ON_DEVICE, EACCES},
+        {ERROR_DIR_NOT_EMPTY, ENOTEMPTY},
+        {ERROR_NOT_LOCKED, EACCES},
+        {ERROR_BAD_PATHNAME, ENOENT},
+        {ERROR_MAX_THRDS_REACHED, EAGAIN},
+        {ERROR_LOCK_FAILED, EACCES},
+        {ERROR_ALREADY_EXISTS, EEXIST},
+        {ERROR_FILENAME_EXCED_RANGE, ENOENT},
+        {ERROR_NESTING_NOT_ALLOWED, EAGAIN},
+        {ERROR_NOT_ENOUGH_QUOTA, ENOMEM}
+    };
+
+    for (const auto &entry : errorTable) {
+        if (win32Error == entry.win32Code) {
+            errno = entry.posixErrno;
+            return;
+        }
+    }
+
+    if (win32Error >= ERROR_WRITE_PROTECT && win32Error <= ERROR_SHARING_BUFFER_EXCEEDED)
+        errno = EACCES;
+    else if (win32Error >= ERROR_INVALID_STARTING_CODESEG && win32Error <= ERROR_INFLOOP_IN_RELOC_CHAIN)
+        errno = ENOEXEC;
+    else
+        errno = EINVAL;
+}
+
 static Mem::Allocator *
 squidaio_get_pool(int size)
 {
@@ -779,7 +849,7 @@ squidaio_do_read(squidaio_request_t * requestp)
 
     if (!ReadFile((HANDLE)_get_osfhandle(requestp->fd), requestp->bufferp,
                   requestp->buflen, (LPDWORD)&requestp->ret, nullptr)) {
-        WIN32_maperror(GetLastError());
+        squidaio_maperror(GetLastError());
         requestp->ret = -1;
     }
 
@@ -821,7 +891,7 @@ squidaio_do_write(squidaio_request_t * requestp)
 {
     if (!WriteFile((HANDLE)_get_osfhandle(requestp->fd), requestp->bufferp,
                    requestp->buflen, (LPDWORD)&requestp->ret, nullptr)) {
-        WIN32_maperror(GetLastError());
+        squidaio_maperror(GetLastError());
         requestp->ret = -1;
     }
 
