diff --git a/pristine/squid-7.5/src/defines.h b/patched/squid-7.5/src/defines.h
index 2d650ed..be0d839 100644
--- a/pristine/squid-7.5/src/defines.h
+++ b/patched/squid-7.5/src/defines.h
@@ -144,7 +144,7 @@
 #endif
 
 /* CygWin & Windows NT Port */
-#if _SQUID_WINDOWS_
+#if _SQUID_WINDOWS_ || _SQUID_MINGW_ /* squid4win-mingw-service-defines */
 #define _WIN_SQUID_SERVICE_CONTROL_STOP SERVICE_CONTROL_STOP
 #define _WIN_SQUID_SERVICE_CONTROL_SHUTDOWN SERVICE_CONTROL_SHUTDOWN
 #define _WIN_SQUID_SERVICE_CONTROL_INTERROGATE SERVICE_CONTROL_INTERROGATE
diff --git a/pristine/squid-7.5/src/dns/rfc1035.cc b/patched/squid-7.5/src/dns/rfc1035.cc
index 6c5901a..aa4a12a 100644
--- a/pristine/squid-7.5/src/dns/rfc1035.cc
+++ b/patched/squid-7.5/src/dns/rfc1035.cc
@@ -58,6 +58,7 @@ rfc1035HeaderPack(char *buf, size_t sz, rfc1035_message * hdr)
     int off = 0;
     unsigned short s;
     unsigned short t;
+    (void)sz;
     assert(sz >= 12);
     s = htons(hdr->id);
     memcpy(buf + off, &s, sizeof(s));
@@ -107,6 +108,7 @@ rfc1035LabelPack(char *buf, size_t sz, const char *label)
     auto len = strlen(label);
     if (len > RFC1035_MAXLABELSZ)
         len = RFC1035_MAXLABELSZ;
+    (void)sz;
     assert(sz >= len + 1);
     *(buf + off) = (char) len;
     off++;
diff --git a/pristine/squid-7.5/src/log/file/log_file_daemon.cc b/patched/squid-7.5/src/log/file/log_file_daemon.cc
index 91f62dc..7589372 100644
--- a/pristine/squid-7.5/src/log/file/log_file_daemon.cc
+++ b/patched/squid-7.5/src/log/file/log_file_daemon.cc
@@ -27,6 +27,9 @@
 #if HAVE_PATHS_H
 #include <paths.h>
 #endif
+#if _SQUID_MINGW_ && !defined(_PATH_DEVNULL)
+#define _PATH_DEVNULL "NUL" /* squid4win-mingw-devnull */
+#endif
 
 #include "helper/protocol_defines.h"
 
diff --git a/pristine/squid-7.5/src/main.cc b/patched/squid-7.5/src/main.cc
index 0f74962..39d05b2 100644
--- a/pristine/squid-7.5/src/main.cc
+++ b/patched/squid-7.5/src/main.cc
@@ -42,6 +42,7 @@
 #include "fs_io.h"
 #include "FwdState.h"
 #include "globals.h"
+#include "win32.h" /* squid4win-mingw-main-win32 */
 #include "htcp.h"
 #include "http/Stream.h"
 #include "HttpHeader.h"
@@ -170,7 +171,7 @@ static void setEffectiveUser(void);
 static void SquidShutdown(void);
 static void mainSetCwd(void);
 
-#if !_SQUID_WINDOWS_
+#if !(_SQUID_WINDOWS_ || _SQUID_MINGW_) /* squid4win-mingw-main-guards */
 static const char *squid_start_script = "squid_start";
 #endif
 
@@ -299,7 +300,7 @@ SignalEngine::handleStoppedChild()
     // does not depend on Squid state. It does not need debugging because it
     // handles an "internal" signal, not an external/admin command.
     do_handle_stopped_child = 0;
-#if !_SQUID_WINDOWS_
+#if !(_SQUID_WINDOWS_ || _SQUID_MINGW_) /* squid4win-mingw-main-guards */
     PidStatus status;
     pid_t pid;
 
@@ -699,7 +700,7 @@ rotate_logs(int sig)
 {
     do_rotate = 1;
     RotateSignal = sig;
-#if !_SQUID_WINDOWS_
+#if !(_SQUID_WINDOWS_ || _SQUID_MINGW_) /* squid4win-mingw-main-guards */
 #if !HAVE_SIGACTION
 
     signal(sig, rotate_logs);
@@ -713,7 +714,7 @@ reconfigure(int sig)
 {
     do_reconfigure = 1;
     ReconfigureSignal = sig;
-#if !_SQUID_WINDOWS_
+#if !(_SQUID_WINDOWS_ || _SQUID_MINGW_) /* squid4win-mingw-main-guards */
 #if !HAVE_SIGACTION
 
     signal(sig, reconfigure);
@@ -727,7 +728,7 @@ master_revive_kids(int sig)
     ReviveKidsSignal = sig;
     do_revive_kids = true;
 
-#if !_SQUID_WINDOWS_
+#if !(_SQUID_WINDOWS_ || _SQUID_MINGW_) /* squid4win-mingw-main-guards */
 #if !HAVE_SIGACTION
     signal(sig, master_revive_kids);
 #endif
@@ -741,7 +742,7 @@ master_shutdown(int sig)
     do_shutdown = 1;
     ShutdownSignal = sig;
 
-#if !_SQUID_WINDOWS_
+#if !(_SQUID_WINDOWS_ || _SQUID_MINGW_) /* squid4win-mingw-main-guards */
 #if !HAVE_SIGACTION
     signal(sig, master_shutdown);
 #endif
@@ -759,7 +760,7 @@ shut_down(int sig)
         shutdown_status = EXIT_FAILURE;
 #endif
 
-#if !defined(_SQUID_WINDOWS_) && !defined(HAVE_SIGACTION)
+#if !defined(_SQUID_WINDOWS_) && !defined(_SQUID_MINGW_) && !defined(HAVE_SIGACTION) /* squid4win-mingw-main-guards */
     signal(sig, shut_down);
 #endif
 }
@@ -769,7 +770,7 @@ sig_child(int sig)
 {
     do_handle_stopped_child = 1;
 
-#if !defined(_SQUID_WINDOWS_) && !defined(HAVE_SIGACTION)
+#if !defined(_SQUID_WINDOWS_) && !defined(_SQUID_MINGW_) && !defined(HAVE_SIGACTION) /* squid4win-mingw-main-guards */
     signal(sig, sig_child);
 #else
     (void)sig;
@@ -1066,7 +1067,9 @@ mainSetCwd(void)
 {
     if (Config.chroot_dir && !Chrooted) {
         Chrooted = true;
-
+#if _SQUID_MINGW_
+        fatalf("chroot_dir is not supported on native MinGW builds");
+#else
         if (chroot(Config.chroot_dir) != 0) {
             int xerrno = errno;
             fatalf("chroot to %s failed: %s", Config.chroot_dir, xstrerr(xerrno));
@@ -1074,6 +1077,7 @@ mainSetCwd(void)
 
         if (!mainChangeDir("/"))
             fatalf("chdir to / after chroot to %s failed", Config.chroot_dir);
+#endif
     }
 
     if (Config.coredump_dir && strcmp("none", Config.coredump_dir) != 0) {
@@ -1113,7 +1117,7 @@ mainInitialize(void)
     debugs(1, DBG_CRITICAL, "Starting Squid Cache version " << version_string << " for " << CONFIG_HOST_TYPE << "...");
     debugs(1, Critical(5), "Service Name: " << service_name);
 
-#if _SQUID_WINDOWS_
+#if (_SQUID_WINDOWS_ || _SQUID_MINGW_) /* squid4win-mingw-main-guards */
     if (WIN32_run_mode == _WIN_SQUID_RUN_MODE_SERVICE) {
         debugs(1, DBG_CRITICAL, "Service command line is: " << WIN32_Service_Command_Line);
     } else
@@ -1127,7 +1131,7 @@ mainInitialize(void)
     setSystemLimits();
     debugs(1, Important(8), "With " << Squid_MaxFD << " file descriptors available");
 
-#if _SQUID_WINDOWS_
+#if (_SQUID_WINDOWS_ || _SQUID_MINGW_) /* squid4win-mingw-main-guards */
 
     debugs(1, DBG_IMPORTANT, "With " << _getmaxstdio() << " CRT stdio descriptors available");
 
@@ -1738,7 +1742,7 @@ sendSignal(void)
     /* signal successfully sent */
 }
 
-#if !_SQUID_WINDOWS_
+#if !(_SQUID_WINDOWS_ || _SQUID_MINGW_) /* squid4win-mingw-main-guards */
 /*
  * This function is run when Squid is in daemon mode, just
  * before the parent forks and starts up the child process.
@@ -1895,7 +1899,7 @@ masterExit()
 static void
 watch_child(const CommandLine &masterCommand)
 {
-#if !_SQUID_WINDOWS_
+#if !(_SQUID_WINDOWS_ || _SQUID_MINGW_) /* squid4win-mingw-main-guards */
     pid_t pid;
 #ifdef TIOCNOTTY
 
diff --git a/pristine/squid-7.5/src/windows_service.cc b/patched/squid-7.5/src/windows_service.cc
index bc436da..d271bef 100644
--- a/pristine/squid-7.5/src/windows_service.cc
+++ b/patched/squid-7.5/src/windows_service.cc
@@ -16,7 +16,7 @@
 #include "tools.h"
 #include "windows_service.h"
 
-#if _SQUID_WINDOWS_
+#if _SQUID_WINDOWS_ || _SQUID_MINGW_ /* squid4win-mingw-windows-service-includes */
 #if !defined(_MSWSOCK_)
 #include <mswsock.h>
 #endif
diff --git a/pristine/squid-7.5/src/windows_service.h b/patched/squid-7.5/src/windows_service.h
index a872267..e0f4294 100644
--- a/pristine/squid-7.5/src/windows_service.h
+++ b/patched/squid-7.5/src/windows_service.h
@@ -9,7 +9,7 @@
 #ifndef SQUID_SRC_WINDOWS_SERVICE_H
 #define SQUID_SRC_WINDOWS_SERVICE_H
 
-#if _SQUID_WINDOWS_
+#if _SQUID_WINDOWS_ || _SQUID_MINGW_ /* squid4win-mingw-windows-service */
 int WIN32_StartService(int, char **);
 int WIN32_Subsystem_Init(int *, char ***);
 void WIN32_sendSignal(int);
